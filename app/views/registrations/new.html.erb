<% if @current_user %>
    <h1>Create New Account</h1>
    <%= render 'form', registration: @registration %>
<% else %>
    <p>Please log in to existing account to create new account.</p>
    <%= render 'form', session: @session %>
<% end %>